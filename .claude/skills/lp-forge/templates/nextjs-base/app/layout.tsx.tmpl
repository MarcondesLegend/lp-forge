import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "{{seoTitle}}",
  description: "{{seoDescription}}",
  openGraph: {
    title: "{{seoTitle}}",
    description: "{{seoDescription}}",
    locale: "{{ogLocale}}",
    images: [{ url: "{{ogImage}}" }]
  },
  robots: "index, follow"
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="{{htmlLang}}">
      <head>
        {/*
          Generated by lp-forge v{{lpForgeVersion}}
          Source URL: {{sourceUrl}}
          Generated at: {{generatedAt}}
          Brand assets: {{brandAssetsMode}}
          Direction: {{direction}}
          Run ID: {{runId}}
        */}
      </head>
      <body>{children}</body>
    </html>
  );
}
